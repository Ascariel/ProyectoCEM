

<div class="col-md-12 text-center">
  <h3>Familias Asociadas</h3>
  <br><br>
</div>

<br>

<table id="projects_table" class="table table-striped table-hover " style="margin-top: 20px">
  <thead>
    <tr>
      <th>ID</th>
      <th>Nombre</th>
      <th>CEL</th>
      <th>Pais</th>
      <th>Estado Postulacion</th>
      <th>Acciones</th>


      <th></th>
    </tr>

  </thead>
  <tbody>
    <% @familias.each do |familia| %>
      <tr>

        <td><%= familia.id %></td>
        <td><%= familia.nombre.titleize %></td>
        <td><%= familia.cel.nombre.titleize %></td>
        <td><%= familia.pais.nombre.titleize %></td>
        <td><%= familia.estado_postulacion_cel.upcase%></td>

        <!-- ACCIONES SEGUN PERFIL -->
          <% if @current_user.es_cel? %>
            <td>

              <a href="/familias/<%= familia.id %>/edit" class="btn btn-warning btn-sm">Editar Postulacion</a>
              <a href="/familias/<%= familia.id %>" class="btn btn-info btn-sm ">Ver</a>
              <a href="/familias/<%= familia.id %>" data-method="DELETE" class="btn btn-danger btn-sm">Eliminar</a>
            </td>
          <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<br><br><br><br><br><br><br>



<% content_for :javascript do %>

  <script type="text/javascript">
//    $(document).ready( function () {
        //$('#projects_table').DataTable({
          //dom: 'f'
        //});
    //});
  </script>
<% end %>
